# Restaurant chatbot

A portable C++17 console ordering application.

```powershell
g++ -std=c++17 -Wall -Wextra -pedantic main.cpp -o restaurant.exe
.\restaurant.exe
```

Customer names are restricted before being used as local filenames. Generated customer/order files are excluded by the repository `.gitignore`.
